import React from 'react'
import styles from './Header.module.css'

export const Header = () => {
  return (
    <div className={`bg-primary text-white text-center ${styles.header}`}>Question Form</div>
  )
}
